syntax = "proto3";

package marketdata;

message StockRequest {
  string symbol = 1;
}

message StockPrice {
  string symbol = 1;
  double price = 2;
  int64 timestamp_ns = 3; // nanosecond resolution
}

service MarketData {
  rpc Subscribe(StockRequest) returns (stream StockPrice);
}